{
  "name": "sellers-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that handles GitHub OAuth callback for Sveltia/Decap CMS on sellersadopt.com",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "@vitest/coverage-istanbul": "^2.1.9",
    "typescript": "^5.6.0",
    "vitest": "2.1.9",
    "wrangler": "^3.90.0"
  }
}
